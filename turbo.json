{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalDependencies": [".env*"], 
  "tasks": {
    "build": {
      "env": ["GW_API_URL_LOCAL", "GW_API_URL_REMOTE", "GW_USER_ID_LOCAL", "GW_USER_ID_REMOTE"],
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "deploy:workers": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
